% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b_center.R
\name{bcenter}
\alias{bcenter}
\title{Center Numeric Variables in a Data Frame}
\usage{
bcenter(df, na.rm = TRUE, exclude = NULL)
}
\arguments{
\item{df}{A data frame}

\item{na.rm}{Logical. Should missing values be removed when calculating means? Default is TRUE}

\item{exclude}{Character vector of column names to exclude from centering. Default is NULL}
}
\value{
A data frame with numeric columns centered (mean = 0) and non-numeric columns unchanged
}
\description{
This function centers all numeric variables in a data frame by subtracting
the mean from each numeric column. Non-numeric columns are left unchanged.
}
\examples{
# Create sample data
df <- data.frame(
  id = c("A", "B", "C", "D"),
  x = c(1, 2, 3, 4),
  y = c(10, 20, 30, 40),
  group = factor(c("G1", "G1", "G2", "G2"))
)

# Center numeric variables
centered_df <- bcenter(df)

}
