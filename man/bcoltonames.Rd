% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b_coltonames.R
\name{bcoltonames}
\alias{bcoltonames}
\title{Convert a column to row names}
\usage{
bcoltonames(df, col = NULL, remove = TRUE)
}
\arguments{
\item{df}{A data.frame}

\item{col}{Column to convert to row names. Can be numeric index or name.
If NULL (default), automatically selects based on heuristics.}

\item{remove}{Logical. Remove the column after conversion. Default TRUE.}
}
\value{
data.frame with modified row names
}
\description{
Convert a column to row names
}
\examples{
\dontrun{
# Auto-detect column to use as row names
df <- bcoltonames(df)

# Specify column by name
df <- bcoltonames(df, "ID")

# Specify column by index
df <- bcoltonames(df, 1)

# Keep the column after setting row names
df <- bcoltonames(df, "ID", remove = FALSE)
}
}
