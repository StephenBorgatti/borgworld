% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b_readgoogle.R
\name{breadgoogle}
\alias{breadgoogle}
\title{Read Google Spreadsheet with borgworld conventions}
\usage{
breadgoogle(
  sheet_url,
  sheet = NULL,
  col = NULL,
  remove_col = TRUE,
  skip = 0,
  na = c("", "NA"),
  col_types = NULL,
  auth = TRUE,
  ...
)
}
\arguments{
\item{sheet_url}{URL or ID of the Google Sheet}

\item{sheet}{Name or index of the specific sheet to read (default is NULL for first sheet)}

\item{col}{Column to convert to row names. Can be numeric index, name, or NULL for auto-detect.
Set to FALSE to skip row name conversion entirely.}

\item{remove_col}{Logical. Remove the column after setting as row names (default TRUE)}

\item{skip}{Number of rows to skip before reading data (default is 0)}

\item{na}{Character vector of strings to interpret as NA (default is c("", "NA"))}

\item{col_types}{Column specification (default is NULL for auto-detection)}

\item{auth}{Should googlesheets4 authenticate? Set to FALSE for public sheets (default is TRUE)}

\item{...}{Additional arguments passed to googlesheets4::read_sheet}
}
\value{
A data frame with appropriate row names set if requested
}
\description{
Loads a Google spreadsheet and optionally converts a column to row names
using bcoltonames, similar to how bpaste works.
}
\examples{
\dontrun{
# Read a public Google Sheet with auto row name detection
df <- breadgoogle("https://docs.google.com/spreadsheets/d/...", auth = FALSE)

# Read without row name conversion
df <- breadgoogle("sheet_id", col = FALSE)

# Read specific sheet and use column "Sample_ID" as row names
df <- breadgoogle("sheet_id", sheet = "Data", col = "Sample_ID")

# Read and use first column as row names
df <- breadgoogle("sheet_id", col = 1)
}
}
