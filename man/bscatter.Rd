% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b_plots.R
\name{bscatter}
\alias{bscatter}
\title{Scatterplot with optional color and regression line}
\usage{
bscatter(df, y, x, color_by = NULL, add_lm = TRUE, se = TRUE)
}
\arguments{
\item{df}{A data frame or tibble.}

\item{y, x}{Column names for the axes (unquoted).}

\item{color_by}{Optional column for point colors (unquoted).}

\item{add_lm}{Logical; if TRUE add a regression line. Default TRUE.}

\item{se}{Logical; if TRUE include confidence band around regression line. Default TRUE.}
}
\value{
A \code{ggplot} object.
}
\description{
Create a scatterplot of two columns of a data frame. Optionally
color points by a third variable, and add a global OLS regression line
(\code{geom_smooth(method = "lm")}) for the whole dataset (not per color group).
}
\examples{
if (requireNamespace("palmerpenguins", quietly = TRUE)) {
  data(penguins, package = "palmerpenguins")
  # Simple scatter
  bscatter(penguins, body_mass_g, flipper_length_mm)

  # Colored by species, with regression line
  bscatter(penguins, fbody_mass_g, flipper_length_mm, color_by = species)

  # Without SE band
  bscatter(penguins, body_mass_g, flipper_length_mm, species, se = FALSE)
}
}
