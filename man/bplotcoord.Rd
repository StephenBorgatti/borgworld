% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b_plotcoord.R
\name{bplotcoord}
\alias{bplotcoord}
\title{Create MDS-style plot with optional clustering colors}
\usage{
bplotcoord(
  coords,
  labels = NULL,
  color = NULL,
  main = "MDS Plot",
  cex = 1,
  cex.text = 0.8,
  pch = 19,
  pos = "auto",
  ...
)
}
\arguments{
\item{coords}{A matrix or data frame of coordinates (n x 2), or an MDS object
with a \code{points} component (from cmdscale, isoMDS, etc.)}

\item{labels}{Character vector of point labels. If NULL, uses row names of
coords or numeric indices.}

\item{color}{Optional factor or character vector for coloring points by group.
Typically cluster assignments or categorical variables.}

\item{main}{Character string for plot title. Default is "MDS Plot".}

\item{cex}{Numeric expansion factor for point size. Default is 1.}

\item{cex.text}{Numeric expansion factor for label text size. Default is 0.8.}

\item{pch}{Plotting character (symbol) for points. Default is 19 (solid circle).}

\item{pos}{Position of labels relative to points. Can be:
\itemize{
\item "auto" (default): Smart positioning based on location in plot
\item 1: Below point
\item 2: Left of point
\item 3: Above point
\item 4: Right of point
}}

\item{...}{Additional graphical parameters passed to \code{plot()}.}
}
\value{
Invisibly returns a list containing:
\item{coords}{The coordinate matrix used for plotting}
\item{colors}{Vector of colors used for each point}
\item{labels}{Vector of labels used}
}
\description{
Creates a scatter plot of MDS coordinates with labeled points.
Optionally colors points and labels by a grouping variable (e.g., clusters).
Automatically adjusts label positions to avoid overlap with plot edges.
}
\examples{
\dontrun{
# Basic MDS plot
mds_coords <- cmdscale(cities)
bplotcoord(mds_coords)

# With clustering colors
bplotcoord(mds_coords, color = citiesattr$cluster)

# Custom labels and parameters
bplotcoord(mds_coords,
          labels = citiesattr$name,
          color = citiesattr$region,
          main = "US Cities by Region",
          cex = 1.2,
          cex.text = 0.7)

# Manual label positioning
bplotcoord(mds_coords,
          color = citiesattr$cluster,
          pos = 1)  # All labels below points
}

}
